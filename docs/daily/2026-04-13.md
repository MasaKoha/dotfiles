# 2026-04-13 日報

## 今日やったこと

### dotfiles — Claude Code コマンド管理基盤の構築

汎用 Claude Code スキル（`/report`）を dotfiles リポジトリで管理し、`~/.claude/commands/` にシンボリックリンクで展開する仕組みを整備した。

- ✅ **`claude/commands/report.md` を dotfiles に移管**
  汎用日報スキルの実体を dotfiles に置き、`~/.claude/commands/report.md` をシンボリックリンクに切り替え。
- ✅ **`scripts/setup-claude-commands.sh` を実装**
  `dotfiles/claude/commands/*.md` を走査してシンボリックリンクを自動構築するスクリプト。既存ファイルはバックアップしてから上書きする設計。
- ✅ **`setup.sh` をエントリポイントとして追加**
  新しいマシンで `bash setup.sh` を叩くだけでセットアップ完了する形に整備。
- ✅ **GitHub へプッシュ**（MasaKoha/dotfiles）

### pisuke — Claude Code コマンド整理

- ✅ **`/daily-report` を `/pisuke-report` にリネーム**（pisuke プロジェクト専用コマンドとして明確化）
- ✅ **汎用 `/report` コマンドを新規作成**（カレントリポジトリ対象の日報スキル）

## 詰まったこと・気づき

- pisuke 専用の `pisuke-report.md` はローカル実ファイルのまま残す設計にしたことで、dotfiles の symlink 管理と共存できた。汎用コマンドだけ dotfiles 管理にするというルールが明確で扱いやすい。

## 明日やること

- TextSlash GDD の未設計セクションを進める
  - 街スキル（職業スキル）の詳細設計
  - ゴールド経済設計
  - 無限フロアの設計
- `master-tasks.md` を Emotia → TextSlash 仕様に更新

## メモ

- `setup.sh` は冪等（同じシンボリックリンクが既にあれば skip）なので、新しいコマンドを dotfiles に追加した後は `setup.sh` を再実行するだけで反映される。
- pisuke-report.md はローカル専用のため dotfiles には含めない。機密情報を含まない汎用コマンドのみ dotfiles で管理する方針。
